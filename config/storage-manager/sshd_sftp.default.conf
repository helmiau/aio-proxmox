# OpenSSH SFTP Configuration (appended to /etc/ssh/sshd_config)
# Storage Manager — SFTP

Match Group storage-users
    ChrootDirectory %h
    ForceCommand internal-sftp
    AllowTcpForwarding no
    X11Forwarding no