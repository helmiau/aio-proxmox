if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"#if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"!if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"/if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"/if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"#if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"yif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"âif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"†if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"’if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"#if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)",if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)",if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)",if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)",if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)",if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)",if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)",if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"#if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"yif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"(if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"0if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"/if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"#if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"(if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"1if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"2if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"!if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"1if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"|if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"|if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"|if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"|if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"|if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"|if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"|if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"?if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"qif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"0if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"yif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"*if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"kif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)":if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"1if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"#if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"(if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"1if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"^if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"^if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"!if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"zif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"kif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"^if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"^if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"!if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"#if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"<if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)">if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"zif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"^if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"^if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"1if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"kif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"(if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)",if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"(if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)",if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"(if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"?if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"/if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"/if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)":if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"|if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"|if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"|if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)":if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"*if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"1if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"yif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"kif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"*if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"kif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)":if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"1if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"#if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"(if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"1if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"#if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"^if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"^if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"^if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"^if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"!if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"!if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"&if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"&if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"#if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"/if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"/if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"/if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"/if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"jif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"jif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"qif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"'if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"(if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"'if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)">if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"&if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"&if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"yif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"#if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"(if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"1if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"yif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)",if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"kif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"#if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"(if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"1if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"yif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)",if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"#if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"(if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"1if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"#if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"(if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"1if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"^if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"^if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"#if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"9if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"&if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"&if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"9if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"&if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"&if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Kif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Kif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"&if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"&if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Kif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Kif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"&if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"&if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"yif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Yif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"kif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Kif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"!if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)":if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"!if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)":if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"!if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)":if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"yif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"!if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"yif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)":if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"!if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)":if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"!if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)":if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"kif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"!if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"kif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)":if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"!if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)":if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"!if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)":if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"zif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"|if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"|if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"zif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"|if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"|if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"zif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"|if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"|if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"zif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"yif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"1if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"#if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"kif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"yif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"|if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"qif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"^if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"yif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"0if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"(if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)")if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"#if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"!if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)":if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)":if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)":if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"\if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"\if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"yif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"\if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"kif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"kif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"\if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"\if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"0if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"0if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)",if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)",if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"/if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)",if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"yif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"\if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"8if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"8if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"8if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"8if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"\if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"zif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"/if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"\if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"wif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"\if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"kif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"yif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Kif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Yif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)":if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"?if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"qif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"0if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"yif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"gif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Xif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"1if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"#if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Mif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"yif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"pif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"{if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"_if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"[if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"0if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"}if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"=if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"$if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"0if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"]if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)";if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"yif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"dif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"Uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)":if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"uif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)" if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"lif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"bif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"/if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"rif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"vif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"eif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"-if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"aif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"cif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"tif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"oif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"nif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)".if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"sif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"hif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)""if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
if [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"fif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"iif [[ -z "\" || -z "\" || -z "\" || -z "\" ]]; then
        log_error "Missing LXC configuration for \"
        return 1
    fi

    # Check if running on Proxmox host (pct command available)
    if ! command -v pct >/dev/null 2>&1; then
        log_error "pct command not found — must run on Proxmox host"
        return 1
    fi

    log_info "Creating LXC container \ (CTID will be assigned)"
